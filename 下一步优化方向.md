## 1. 减小跳表锁粒度
## 2. 在持久化功能上添加双文件写+原子重命名，防止持久化过程中断电导致数据损坏（rename由文件系统保证原子性）
## 3. 协程超时任务被晚触发了，产生无谓的频繁选举。采用预投票机制，节点在宣告成为 Candidate 并无脑增加真实 Term 之前，先以当前的 Term 向大家发送一轮“预投票”。在这个阶段如果其他人能连上 Leader，就会反驳这个包。这避免了网络不稳导致不必要的集群任期号暴涨与系统动荡。
## 4. 弃用ucontext，改用更轻量级的协程库，减少上下文切换开销。
## 5. N-M任务队列有锁竞争，改用thread_local加工作窃取。