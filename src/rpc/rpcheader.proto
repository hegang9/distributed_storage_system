syntax = "proto3";

package RPC;

message RpcHeader
{
    bytes service_name = 1;// 要调用的服务名称
    bytes method_name = 2;// 要调用的方法名称
    uint32 args_size = 3; // 参数数据的长度 (参数体本身的序列化长度)这里虽然是uint32，但是protobuf编码的时候默认就是变长编码
}